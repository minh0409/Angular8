import { ElementRef } from '@angular/core';
/**
 * @hidden
 */
export declare class NavigationMetadata {
    dataRows: number;
    headerRows: number;
    isVirtual: boolean;
    hasPager: boolean;
    gridElement: ElementRef;
    readonly maxLogicalRowIndex: number;
    constructor(dataRows: number, headerRows: number, isVirtual: boolean, hasPager: boolean, gridElement: ElementRef);
}
