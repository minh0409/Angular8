import { EventEmitter, ElementRef, OnDestroy, NgZone, OnChanges, OnInit } from '@angular/core';
/**
 * @hidden
 */
export declare class DraggableDirective implements OnInit, OnChanges, OnDestroy {
    private element;
    private ngZone;
    enableDrag: boolean;
    kendo: {
        drag: EventEmitter<any>;
        press: EventEmitter<any>;
        release: EventEmitter<any>;
    };
    private draggable;
    constructor(element: ElementRef, ngZone: NgZone);
    ngOnInit(): void;
    ngOnChanges(changes: any): void;
    ngOnDestroy(): void;
    private toggleDraggable();
    private destroyDraggable();
}
